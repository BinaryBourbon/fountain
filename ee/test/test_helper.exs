# Intentionally empty. ExUnit, SQL sandbox mode, and Mimic copies are all
# initialised by apps/fountain/test/test_helper.exs, which Mix loads first
# (test_paths order in apps/fountain/mix.exs). Mix requires a test_helper.exs
# in every test_paths entry, so this file must exist even though it does nothing.
