apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fountain-tls
  namespace: fountain
spec:
  secretName: fountain-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - fountain-hc.inevitable.fyi
