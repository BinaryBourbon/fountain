# Deploy

## First run

Run it once. The install step is on the [setup page](../setup.md#install).

!!! note
    A note with no custom title, holding a fence:

    ```json
    {"ok": true}
    ```

## Duplicate heading

## Duplicate heading

Comrak gives the second one the id `duplicate-heading-1`; see [it](#duplicate-heading-1).

<figure>
<svg viewBox="0 0 10 10"><rect fill="#b3760f"/></svg>
<figcaption>A diagram the trusted path keeps.</figcaption>
</figure>
