# The fixture manual

A small manual that exercises every part of the dialect. Start with
[setup](setup.md), then the [deploy guide](guides/deploy.md#first-run).

!!! tip "In a hurry?"
    Skip to [the first run](guides/deploy.md#first-run).

    It is two commands.

Fenced code in a baked language:

```elixir
def run(x), do: x
```
